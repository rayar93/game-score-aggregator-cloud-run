# --- secrets: never commit these ---
.env
.igdb_token.json

# --- importer resume state ---
.igdb_bulk_progress.json

# --- local database files ---
*.db
*.db-wal
*.db-shm

# --- python ---
__pycache__/
*.pyc
.venv/
venv/

# --- GCP / cloud credentials ---
secrets/
*-key.json
service-account*.json

# --- editor / OS stuff ---
.vscode/
.DS_Store
Thumbs.db

# --- Cloud SQL Auth Proxy - personal dev tool, install outside the repo ---
cloud-sql-proxy
cloud-sql-proxy.exe
cloud-sql-proxy.x64.exe
*.x64.exe
# --- frozen snapshot ---
# 423 MB, over GitHub's 100 MB file limit. Distributed as a
# Release asset instead; see README.
gamedb.sqlite
*.sqlite
*.sqlite-wal
*.sqlite-shm
gamedb-full.sql.gz
